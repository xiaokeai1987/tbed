name = "shared-image-site"
compatibility_date = "2025-12-01"
pages_build_output_dir = "."

## 说明：
## Pages 正式环境的 KV/D1 绑定请在仪表盘中配置（Functions → Bindings）
## 本地开发请使用 `wrangler pages dev .` 的模拟绑定或在仪表盘中创建后再运行
